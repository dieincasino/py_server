services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: py_server_backend
    expose:
      - "8080"
    networks:
      - app-network

  nginx:
    image: nginx:1.27-alpine
    container_name: py_server_nginx
    depends_on:
      - backend
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

